/*. Diseña una clase Alumno ””, en ella se guardaran los atributos DNI ””, nombre ””, un
mapa asignatura nota ” y notaMedia ””. Implementa métodos en Alumno para que al
modificar las notas, se modifique la nota media. */

class Alumno {
    constructor(dni, nombre,asignaturas, notaMedia) {
        this.dni=dni
        this.nombre=nombre
        //this.
    }
}